[Unit]
Description=Run smoke Postgres backup daily

[Timer]
# 05:30 UTC, 30 min before the 06:00 scrape, to capture a clean pre-scrape state.
OnCalendar=*-*-* 05:30:00 UTC
# Run on next boot if the machine was off at the scheduled time.
Persistent=true

[Install]
WantedBy=timers.target
